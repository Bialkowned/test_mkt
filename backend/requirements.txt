fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic[email]==2.5.0
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
python-multipart==0.0.6
motor==3.3.2
pymongo==4.6.0
python-dotenv>=1.0.0
stripe>=8.0.0

httpx>=0.25.0
aiofiles>=23.0.0

# Transactional mail goes through the fleet SES transport: `bialkowned-ses-transport`,
# which installs a module named `resend` that keeps the resend.Emails.send({...})
# signature ~145 call sites across the fleet already use, and routes to SES.
#
# Referenced by path, and the distribution is deliberately NOT named `resend`. A bare
# `resend` pin resolves against PyPI and installs the real upstream SDK, which overwrites
# the transport and silently returns this program's mail to an API whose credentials were
# decommissioned. 48 programs carried such a pin; this line is what replaced it.
#
# Source: _infra/ses-transport. It declares boto3 itself.
bialkowned-ses-transport @ file:///home/pjb/Production/_infra/wheels/bialkowned_ses_transport-1.1.0-py3-none-any.whl

boto3==1.43.79
