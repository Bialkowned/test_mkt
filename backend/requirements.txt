fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic[email]==2.5.0
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
python-multipart==0.0.6
motor==3.3.2
pymongo==4.6.0
python-dotenv>=1.0.0
stripe>=8.0.0

httpx>=0.25.0
aiofiles>=23.0.0

# Transactional mail goes through the fleet SES shim: a package named `resend`,
# version 1.1.0-bialkowned-ses, that keeps the resend.Emails.send({...}) signature
# ~116 call sites already use and routes the message to SES instead.
#
# It is deliberately NOT pinned here. The shim is not on PyPI, so `pip install resend`
# fetches the real upstream SDK, overwrites the shim, and silently sends this program's
# mail back to an API whose credentials were decommissioned when the fleet moved to
# SES. Rebuilding this venv means reinstalling the shim by hand.
#
# boto3 is what the shim uses to reach SES.
boto3==1.43.79
