# Dev server port
VITE_DEV_PORT=5008

# Backend URL for Vite dev proxy (not used in production — nginx handles it)
VITE_API_TARGET=http://localhost:5108

# Stripe publishable key
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
